For each tree type, implement:

- Basic data structures
- Basic algorithms for manipulation
- BFS, DFS
- Parsing

Parsing example:

- `ParentId,ChildId` data. Pairs of ids.
- `Id,Attribute` or `Id,A1,...,AN` data

